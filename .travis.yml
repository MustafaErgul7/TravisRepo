---
os: linux
language: generic
install: true
addons:
  apt:
    packages:
      - openssh-client

before_install:
  - sudo apt-get uprade

script:
  - ssh -V

jobs:
  include:
    - dist: trusty
    - dist: xenial
    - dist: bionic

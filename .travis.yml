---
os: linux
language: generic
install: true
addons:
  apt:
    update: true
    packages:
      - openssh-client

before_install:
  - sudo apt-get upgrade

script:
  - ssh -V

jobs:
  include:
    - dist: trusty
    - dist: xenial
    - dist: bionic

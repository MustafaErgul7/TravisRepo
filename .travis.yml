---
os: linux
dist: xenial
language: python

install: true

script: true
before_deploy:
  - export $TRAVIS_BUILD_NUMBER=999

deploy:
  provider: script
  script: deploy.sh
  on:
    condition: $TRAVIS_BUILD_NUMBER=999888
